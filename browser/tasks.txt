Task list
=========

make wallet.jsx support multiple wallets
----------------------------------------
+ today it is just a single wallet. It should be "boxes" of wallets,
  and a box for "New wallet". So when starting it first time it only has
  "New wallet" box. clicking on it - adds a new wallet, where you select
  all the things you sellect in today's wallet.jsx implementation:
  Network (Bitcoin, Bitcoin testnet, Lif), the wallet key etc...
+ Once a wallet is added, in its box is the amount of unspent coins it has,
  and amount of BTC/LIF it has.
+ clicking on it - opens up the wallet's transactions, and allows you to
  delete the wallet, and to send and receive from this wallet.
+ in the global settings, you can set the electrumx server for the different
  networks (Bitcoin, Bitcoin testnet, Lif...)
* cache (c) and cache store (cs) for instant statup and screen switching
- Settings screen: move Developer tools to separate screen.
  - Add a Memopool delete button /reset_mempool
- add click to mine
- move BTC test network into "Developer tools"
- add LIF test network, and additional bitcoin test networks.
- in Network options, put LIF by default, and first, BTC second. via Radio
  button.
- create tx with kv_lif controlling outputs with 0 value (like runs)
  (inscribe/edit/transfer)
- never use lif_kv outputs for regular Send tx. Need to do a kv transfer tx
  and then the change will get "separated" back to us.
- fix kv_lif passing for missing 'val'
- show in wallet tx list txs with lif_kv: that they have lif kv in them,
  both in the tx list, and in tx detail list
- dont show in BTC wallets the lif kv options
- open new users in github, and re-import the projects, but with different
  login names/emails

coding conventions:
-------------------
- no spaces around arrow functions:
  GOOD: (a, b)=>code
  BAD: (a, b) => code
- no spaces around === and ==
  GOOD: if (a===null)
  BAD: if (a === null)
- use == for string and simple number comparison
  GOOD: if (a=='test')
  BAD: if (a == 'test')
  BAD: if (a === 'test')
- dont use Promise(). dont use xxx.then(). Use await
  GOOD: await do_long_work(arg)
  BAD: do_long_work(arg).then()
- no statements on same if () line:
  GOOD:
    if (x==1)
      return;
  BAD:
    if (x==1) return;
- no space after async arrow functions:
  GOOD: async()=>...
  BAD: async ()=>...
- always update prompt.txt at end of every task with the prompt and the
  summary ("Done. ...").
